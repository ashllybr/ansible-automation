<!DOCTYPE html>
<html>
<head>
    <title>Ansible Deployed</title>
    <style>
        body { 
            font-family: Arial; 
            text-align: center; 
            padding: 50px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        h1 { font-size: 48px; margin-bottom: 20px; }
        .info { 
            background: rgba(255,255,255,0.1); 
            padding: 30px; 
            border-radius: 10px;
            margin: 30px auto;
            max-width: 600px;
        }
        .info p { font-size: 20px; margin: 10px 0; }
    </style>
</head>
<body>
    <h1>🚀 Automated with Ansible</h1>
    <div class="info">
        <p><strong>Hostname:</strong> {{ ansible_hostname }}</p>
        <p><strong>IP Address:</strong> {{ ansible_default_ipv4.address }}</p>
        <p><strong>OS:</strong> {{ ansible_distribution }} {{ ansible_distribution_version }}</p>
        <p><strong>Deployed:</strong> {{ ansible_date_time.iso8601 }}</p>
    </div>
    <p>This web server was deployed automatically using Ansible automation!</p>
</body>
</html>
